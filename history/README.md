# Superseded baseline and preserved technical work

The First Step v3 replaced the previous creative baseline on 8 September 2026. The old root v1 master in Markdown/PDF/DOCX, workbook, root registers, package manifest and obsolete design-review extracts were removed from the active checkout at the user's request.

They remain recoverable in the local Git history at revision **`1fd1beb`**, the last pre-v3 commit. Use `git show 1fd1beb:<repository-relative-path>` to inspect a historical text file. This note is not a duplicate active blueprint.

PH00/PH01 source, assets, packaged builds and `phase-reviews` are preserved. Their evidence concerns the old requirements; it does not establish acceptance of the new story, sequences or tests. Some historical evidence links name files now available only in that revision. Do not restore those files to the project root as competing authority.

The active creative baseline is [First Step master v3](../blueprint/THE_HELD_First_Step_v3/THE_HELD_First_Step_Master_v3.md). The active development order is [implementation.md](../implementation.md), revision 3.0, draft for review. No new implementation phase started during this documentation migration.
